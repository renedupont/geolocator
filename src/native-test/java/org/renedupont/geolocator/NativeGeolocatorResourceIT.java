package org.renedupont.geolocator;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGeolocatorResourceIT extends GeolocatorResourceTest {

    // Execute the same tests but in native mode.
}